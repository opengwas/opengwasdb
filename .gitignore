__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
# Pixi-managed environments/cache: reproduced from pyproject.toml's
# [tool.pixi.*] tables + pixi.lock, never committed.
.pixi/
dist/
/build/
.DS_Store
docs/benchmark-output/*.opengwasdb/
docs/benchmark-output/*_combined_source.tsv.gz

# transient build logs (build.sh writes + rotates these)
data/**/build.log
data/**/build.log.*
# local Claude Code state (not shared)
.claude/settings.local.json
.claude/*.lock
.claude/worktrees/
quality/.running/
quality/.events.jsonl
